package com.allianz.example;
/*
import com.allianz.example.database.entity.AddressEntity;
import com.allianz.example.mapper.AddressMapper;
import com.allianz.example.model.AddressDTO;
import com.allianz.test.TestMapper;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.util.Assert;

@SpringBootTest
class ExampleApplicationTests implements TestMapper {

    @Autowired
    AddressMapper addressMapper;

    @Test
    void contextLoads() {

    }

    @Test
    @Override
    public void testNullEntityToDtoUpdate() {
        String correctValue = "Hello";
        AddressDTO addressDTO = new AddressDTO();
        addressDTO.setTitle(correctValue);
        AddressEntity testAddressEntity = new AddressEntity();

        System.out.println(addressDTO.getTitle());
        addressDTO = addressMapper.entityToDTO(testAddressEntity);
        System.out.println(addressDTO.getTitle());

        assert (correctValue.equals(addressDTO.getTitle()));
    }
}*/
